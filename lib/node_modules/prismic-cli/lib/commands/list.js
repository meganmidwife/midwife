"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const command_1 = require("@oclif/command");
const prismic_1 = require("./../prismic");
const yeoman_env_1 = require("../prismic/yeoman-env");
class List extends prismic_1.Command {
    async run() {
        const generators = yeoman_env_1.names.map(name => {
            const nameWithOutPrismicPrefix = name.replace('prismic-', '').replace(/js$/i, 'JS');
            return nameWithOutPrismicPrefix.charAt(0).toUpperCase() + nameWithOutPrismicPrefix.slice(1);
        });
        generators.forEach(d => this.log(d));
    }
}
exports.default = List;
List.description = 'List all the available project template generators.';
List.flags = {
    help: command_1.flags.help({ char: 'h' }),
};
