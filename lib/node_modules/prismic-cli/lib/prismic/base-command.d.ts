import { Command } from '@oclif/command';
import { IConfig } from '@oclif/config';
import Prismic from './communication';
export default abstract class PrismicCommand extends Command {
    prismic: Prismic;
    constructor(argv: string[], config: IConfig);
    catch(err: any): Promise<any>;
    login(maybePort?: number, maybeBase?: string, maybeAuthUrl?: string): Promise<void>;
    validateDomain(name: string | undefined, existingRepo?: boolean): Promise<string>;
    validateFolder(name: string | undefined, fallback: string, force: boolean): Promise<string>;
    validateTheme(maybeTheme?: string): Promise<string>;
    isLocalZip(maybePath: string): boolean;
    isAbsoluteUrlToZip(maybeUrl: string): boolean;
    isGithubUrl(maybeUrl: string): boolean;
    maybeGitHubRepo(source: string): Promise<string>;
}
