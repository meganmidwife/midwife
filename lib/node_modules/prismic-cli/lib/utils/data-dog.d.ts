import { AxiosResponse } from 'axios';
import PrismicCommand from './../prismic/base-command';
export default function datadog(error: string | Error, payload: PrismicCommand): Promise<AxiosResponse>;
